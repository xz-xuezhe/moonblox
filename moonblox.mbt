// MoonBlox public API — called from user projects

// Public API is split across sub-packages:
//   @ast       — AST type definitions
//   @convert   — Markdown → AST conversion
//   @parser    — MoonBlock directive parser
//   @render    — AST → Cell tree builder
